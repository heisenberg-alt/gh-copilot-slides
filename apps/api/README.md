# Slide Builder API

FastAPI backend for Slide Builder v2.

## Setup

```bash
# Install dependencies
pip install -e .

# Run development server
uvicorn api.main:app --reload
```

## Environment Variables

See `.env.example` for required configuration.
